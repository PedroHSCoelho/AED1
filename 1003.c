#include <stdio.h>
#include <stdlib.h>

int main() {
    int a, b, total;

    scanf("%d", &a);
    scanf("%d", &b);

    total = a + b;

    printf("SOMA = %d\n", total);

    return 0;
}